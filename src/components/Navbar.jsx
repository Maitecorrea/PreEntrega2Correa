// Make a navbar that at the end shows a component called Cart

import React, { Component } from "react";

class Navbar extends Component {
  render() {
    return (
      // Make a navbar that at the end shows a component called Cart
      <div>
        <nav>
          <ul>
            <li>Tienda</li>
            <li></li>
          </ul>
        </nav>
      </div>
    );
  }
}

export default Navbar;
